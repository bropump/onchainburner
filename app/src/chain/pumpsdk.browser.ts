/** Browser resolution of "#pump-sdk": Vite bundles the SDK's ESM build and
 * its CJS dependencies (anchor, bn.js) with proper interop. */
export { PUMP_SDK, feeSharingConfigPda } from "@pump-fun/pump-sdk";
